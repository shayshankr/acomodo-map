# The sync reads the Sheet's public export — no Google API client or auth needed.
# openpyxl parses the downloaded workbook (values + the Media Link hyperlinks).
# The build scripts (geocode/universities/build_data/fetch_photos) use the stdlib.
openpyxl>=3.1
